主要优化点：
向量数据和TID数据分离上传，提高性能
cuda_wrapper.cu中，并行处理所有查询的TopK选择

小批量问题能输出结果，而且结果正确（测试文件performance_comparison_test.sql）
大批量问题能输出结果，GPU和CPU计算浮点数的差异会导致单次查询和批量查询的答案不同，有一个warning小报错，需要进一步优化（测试文件6.sql）


make clean
make -f Makefile.cuda
cd /root/pgvector &&  cp vector.so /usr/lib/postgresql/16/lib/vector.so &&  service postgresql restart && sleep 2 && pg_isready

cp test_ivfflat_batch/performance_comparison_test.sql /tmp/performance_comparison_test.sql 
cp test_ivfflat_batch/96.sql /tmp/96.sql 

cd /tmp
su postgres
psql -f performance_comparison_test.sql
psql -f 96.sql  

